<div class="text-center"><%= image_tag('logo.png', class: "small") %><h1 class="font-plus"><strong>Dinnder</strong></h1></div>

<div class="text-center">
<a href="/signup" class="btn btn-warning pull-center" >Sign Up</a>
<a href="/login" class="btn btn-primary pull-center">Log In</a>
</div>

<div class="">
  <div class="scroller pull-center">
  <% @search.businesses.each do |f| %>
      <a href="/show/<%= f.id %>"><%= image_tag(f.image_url.gsub!("ms.jpg","l.jpg"), :class => "img_edit centerize") %></a>
  <% end %>
  <% @search1.businesses.each do |f| %>
    <a href="/show/<%= f.id %>"><%= image_tag(f.image_url.gsub!("ms.jpg","l.jpg"), :class => "img_edit centerize" ) %></a>
  <% end %>
  </div>
</div>