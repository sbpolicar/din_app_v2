<nav class="navbar nav-style navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <div>
        <div class="well">
          <% if @current_user %>
            <%= image_tag('logo.png', class: "minimizer")%><%= link_to "Dinnder", main_path %>
          <% else %>
            <%= image_tag('logo.png', class: "minimizer")%><%= link_to "Dinnder", root_path %>
          <% end %>
          <div class="pull-right">

            <% if @current_user %>
              <%= @current_user.display_name %><br>
              <%= link_to "My Favorites", favorites_path, class: "" %><br>
              <%= link_to "Logout",logout_path, class: "" %>
            <% else %>
              <li><a href="/signup" class="btn btn-warning" >Sign Up</a></li>
              <li><a href="/login" class="btn btn-primary">Log In</a></li>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div><!-- /.container-fluid -->
</nav>